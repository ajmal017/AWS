* Link to this file in the description Below
* Host machine is Windows with a IP of 192.168.0.18
* Host machine has Virtual Box and Red Hat Instance is running on Virtual Box with ip 192.168.0.100


yum install mariadb mariadb-server -y;
systemctl start mariadb;
systemctl enable mariadb;
firewall-cmd --permanent --add-service=mysql;
firewall-cmd --reload; 
mysql_secure_installation;

mysql -uroot -ppassword -e "select @version; show databases";mysql -p;

create user user3@'192.168.0.18' identified by 'Sonidg5l!';
grant all privileges on *.* to user3@'192.168.0.18';
flush privileges;
SHOW GRANTS FOR user3@'192.168.0.18';
SELECT user,host FROM mysql.user;

create database testgs1;
CREATE TABLE `testgs1`.`new_table` (   `idnew_table` INT NOT NULL,   `Name` VARCHAR(45) NULL,   `Age` VARCHAR(45) NULL,   `City` VARCHAR(45) NULL,   `Sex` VARCHAR(45) NULL,   `ID` VARCHAR(45) NOT NULL,   PRIMARY KEY (`idnew_table`, `ID`),   UNIQUE INDEX `ID_UNIQUE` (`ID` ASC));

INSERT INTO `testgs1`.`new_table`
(`idnew_table`,
`Name`,
`Age`,
`City`,
`Sex`,
`Id`)
VALUES
(12,'TestUser','80','SaintJoseph','Male','1');

SELECT user,host FROM mysql.user;
quit;

mysqldump -uroot -ppassword testgs1 > testgs1.sql;sleep 3;c;
mysql -uroot -ppassword;

use testgs1;
describe new_table;
show databases;
drop table new_table;
show tables;
quit;

mysql -uroot -ppassword testgs1 < testgs1.sql


vim /etc/my.cnf;
port = 5506

firewall-cmd --permanent --add-port=5506/tcp;
firewall-cmd --reload;
semanage port -a -t mysqld_port_t 5506 -p tcp;
systemctl restart mariadb;
ss -nlp | grep 5506;





